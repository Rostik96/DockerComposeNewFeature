version: '3.1'

services:

  db:
    image: postgres
    restart: always
    ports:
      - "54322:5432"
    environment:
      POSTGRES_DB: people
      POSTGRES_USERNAME: postgres
      POSTGRES_PASSWORD: password
